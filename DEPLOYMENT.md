# RakuXQ 部署

本文件描述当前部署边界。RakuXQ 尚未登记正式域名或生产部署单元，因此目前只允许本地运行和容器验证。

## 当前事实

- 正式域名：`not-configured`
- 服务器：`not-configured`
- 运行方式：本地 Python 或本地容器
- 健康检查：`GET /healthz`
- 图片存储：默认不持久化
- 模型存储：运行时只读挂载，来源和校验信息必须记录在 `models/README.md`

## 生产闸门

首次上线前必须先完成：

1. 在 `registry/projects.yaml` 登记正式域名、服务器逻辑 ID、部署根目录和健康检查。
2. 确定 API 认证、限流、请求体上限、超时和隐私策略。
3. 完成模型许可证、校验和、盲测指标与回滚版本登记。
4. 建立可重复的一键发布、HTTPS、公网冒烟测试和失败回滚入口。

当前不存在获准的一键生产发布命令，也没有发生生产环境变化。

