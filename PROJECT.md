---
document_type: living-project-cognitive-baseline
project_id: rakuxq
baseline_revision: "10"
parent_revision: "9"
change_operation: REVISE
status: active
updated_at: "2026-09-17T20:05:00+08:00"
---

# RakuXQ

- 类型：`computer-vision-api`
- 负责人：`rakulgt / Raku Intelligence（罗酷智能）`

## 产品定位

RakuXQ 是面向开发者、自动化用户和中国象棋应用的开源智能基础设施。第一阶段把手机截图、网页棋盘图和真实棋盘照片可靠转换为中国象棋 FEN；第二阶段在此基础上接入 NNUE 引擎，返回最佳着法、候选变化和可推送结果。

正式域名同时承担项目官网和公开运行面板：匿名展示最近 72 小时的成功识别记录，并保留不含个人或请求标识的历史累计数字，让社区能够看到项目的真实使用、质量门控与持续成长。

项目技术源流始于李国泰长期的象棋实践：2008 年以手工线性评估函数和 Alpha-Beta 搜索开发 lgtXQ，2018 年转向 NNUE 评估与新一代 Alpha-Beta 搜索并使用 RakuXQ 名称，2023 年形成视觉识别原型，2026 年将视觉模块演进为当前开源 HTTP API。当前仓库先公开视觉阶段，历史引擎能力在第二阶段经整理、测试后重新接入。

## 范围、边界与持久不变量

- 第一阶段必须完成：通过稳定 HTTP API 接收图片，检测并校正棋盘，识别 90 个交叉点，输出规范化局面、FEN、置信度和可审计警告。
- 第二阶段预留但第一阶段不实现：NNUE 引擎调度、最佳着法、变化线、解释及终端推送。
- 明确不做：在无法可靠判断时静默猜测棋子、棋盘方向或行棋方；将用户图片默认保存为训练数据；把单一第三方模型永久绑定为唯一实现。
- 必须长期保持：API 与模型实现解耦；低置信度结果必须拒识或要求复核；任意自动修正都保留依据；公开评测以整盘零错误率为核心。
- 自动识别验收只承认服务端视觉 provider 的实际输出；人工标注和多模态大模型只能建立测试集标准答案，不得作为生产识别回退路径或计入模型准确率。
- 用户上传局部棋盘图即视为授权将经几何计算确认未进入画面的棋盘交叉点默认为空位；由该约定造成的局面遗漏由调用者承担，接口必须显式披露假设及受影响坐标，便于用户发现后重拍。
- “画面外默认空”和“画面内未知默认空”只作用于可审计的明确条件：前者要求交叉点中心位于图像边界外，后者要求模型原始类别为未知 `x`。由手部遮挡或非标准外观造成的未知格仍按用户确认的责任规则补空，并保留原始候选与置信度。
- 单格置信度低不能单独把明确棋种删除为空位。画面内已经识别为具体棋种的弱候选应先接受同图重复棋子的视觉原型复核；能保守纠正时记录纠正来源和原始结果，不能纠正时保留候选并要求复核。
- 用户体验目标：Apple 快捷指令和客户程序默认消费完整 JSON，仅在 `status=accepted` 时读取 `fen`；纯文本 `/v1/fen` 作为兼容接口保留。
- 公开运行面板只能展示匿名交互元数据；不得公开原图、Key/Key ID、IP、文件名、请求 ID、设备信息或可反向关联客户的标识。

## 核心产品规则

- 成功响应必须区分 `accepted`、`review_required` 和 `rejected`，不能只返回看似有效的字符串。
- 实体棋盘静态照片通常不包含行棋方信息；没有可信上下文时只返回棋子布局并要求调用者提供 `side_to_move`，不能伪造完整引擎 FEN。
- 自托管版本默认不持久化图片。官方托管服务在用户明确授权下，将已鉴权调用的原图、请求参数、完整响应和诊断元数据保存于服务器短期审计池，最长 12 小时后自动删除；短期记录不等同于训练授权，不得自动进入长期样本集或训练集。
- 模型与数据集必须附带来源、许可证、版本和校验信息，确保开源发布可复现。
- 对部分棋盘重建，响应必须包含 `UNSEEN_CELLS_ASSUMED_EMPTY` 警告和按行列列出的 `assumed_empty_cells`；这类结果可以生成 FEN，但调用者能够区分实拍空位与规则补空。
- 对画面内未知格补空，响应必须包含 `UNCERTAIN_CELLS_ASSUMED_EMPTY`，并将对应坐标原因标记为 `visually_uncertain`；`prediction` 保留模型原始 `x`，顶层 `grid` 表示应用产品规则后的最终局面。
- 对同图视觉原型自动纠正，响应必须包含 `SAME_IMAGE_PROTOTYPE_REFINEMENT`；完整响应保留原类别、原置信度、纠正方法和相似度证据。
- 官方托管 API 为每位客户签发独立密钥并记录独立到期时间；密钥只保存不可逆哈希，过期响应必须披露微信 `lgtqcn`、`39 元人民币/年`和一年续期周期。
- 官方托管短期审计不得记录明文 API Key；只记录 Key ID，并设置 12 小时保留上限和磁盘容量上限。无有效 Key 的请求不得触发图片留存，避免形成匿名存储入口。
- MIT 开源源码与官方托管服务必须明确区分：源码继续免费开放，年费只对应维护者提供的服务器推理、带宽、密钥和运维服务。
- 公开单条交互只保留 72 小时，允许展示时间、状态、简化 FEN、置信度和推理耗时；历史累计仅永久保存汇总计数和首末时间，不永久保存逐条交互。
- 官网提供 Apple 快捷指令下载入口，但公开快捷指令不得内置或泄露任何客户 API Key；下载链接作为服务器配置管理，不写死在页面源码。
- 官网提供面向开发者的调用教程与 6 分钟临时 Key。临时 Key 明文只显示一次、不可续期，同一匿名来源在有效期内只签发一个；服务端只保存加盐不可逆来源指纹，不保存领取者原始 IP。临时 Key 的识别调用仍遵循 12 小时原图审计规则，生产用途继续使用独立年度 Key。
- 匿名交互流的每个有效 FEN 同时提供“复制 FEN”和“在 xiangqiai.com 打开局面”操作；跳转 URL 使用简化 FEN，并将行棋方前的空格编码为 `%20`。

## 架构意图

- 对外维持版本化的图片识别 HTTP API；Apple 快捷指令只是其中一个客户端。
- 识别流水线由可替换 provider 承担，领域层统一完成方向归一化、FEN 构造、合法性检查、置信度门控与响应协议。
- 棋盘定位不能永久依赖四角全部可见；部分棋盘回退路径应利用定位星、九宫斜线、楚河汉界、横纵线交点和固定格距拟合完整 9×10 网格，并生成每个交叉点的可见性掩码。
- 第一阶段允许以可追溯的开源模型建立基线，但最终形成 RakuXQ 自有数据、评测和权重能力。
- 同一图片中存在多个相同字形和材质棋子时，允许用高置信棋子建立临时视觉原型，保守复核低置信明确棋种；该方法不得把空位或未知格提升成棋子，也不能替代未来独立占位检测器和自有分类模型。
- 第二阶段通过稳定的局面契约消费第一阶段输出，不与视觉模型内部结构耦合。
- 匿名公开统计使用独立持久化存储，与 12 小时原图审计池和 API Key 数据库解耦；原图审计清除不影响累计数字，公开面板也不能读取原图。
- 临时 Key 防滥用通过服务端 HMAC 来源指纹、单来源有效期锁、全局活跃上限和入口限流共同实现，不把原始来源地址写入 Key 数据库。

## 关键决策与被否决路径

- 已确认：总品牌使用 `RakuXQ`，机器 slug 使用 `rakuxq`。
- 已确认：项目发起人为李国泰（rakulgt，`lgt@rakubank.com`），公开叙事保留从棋手经历、2008 年 lgtXQ、2018 年 RakuXQ 引擎、2023 年视觉原型到 2026 年开源视觉 API 的技术传承。
- 已确认：源码采用 MIT License，通过公开仓库 `https://github.com/rakulgt/rakuxq` 发布；第三方 ONNX 权重不随源码仓库重新分发。
- 已确认：第一阶段只解决局面识别，第二阶段解决最佳支招。
- 已确认：优先复用开源模型和通用推理框架，不从零重复建设基础算子；差异化投入数据、拒识、整盘评测和工程接口。
- 已确认：局部棋盘图片中未进入画面的交叉点默认无子，风险由主动上传该图片的调用者承担；产品通过显式警告和坐标清单提供可审计性，而不是阻止生成结果。
- 已确认：官方托管入口使用 `https://xq.rakubank.com/v1/recognitions`，部署到 `raku-cn-prod-01`；客户以带有效期的独立 API Key 调用，标准托管价格为 `39 元人民币/年`，续费联系微信 `lgtqcn`。
- 已确认：为支持早期准确率、稳定性和失败模式分析，官方托管服务保存已鉴权交互的原图与完整结果，最长 12 小时后自动清除；它不是永久数据集或训练授权。
- 已确认：`xq.rakubank.com` 不是空白 API 根路径，而是包含实时匿名交互流、累计数据、项目动机、技术来历、路线图与 Apple 快捷指令入口的公开官网。
- 已确认：公共开发者页面允许匿名领取只生效 6 分钟的随机测试 Key，并明确披露一次性显示、短期审计和年度生产 Key 的边界。
- 已否决：以 OCR 作为主识别路线，因为棋子字体、旋转、遮挡和复杂背景会使文字识别缺乏稳定性。
- 已否决：用高单格准确率替代整盘准确率，因为一个错误棋子即可使后续引擎结论失真。

## 产品验收标准

- Apple 快捷指令能够把截图作为表单文件上传、解析 JSON，并在 `status=accepted` 时取得可直接复制的 `fen` 字符串。
- API 对无法形成完整 FEN 的请求返回明确的机器可读原因，不把不确定结果标记为成功。
- API 能从四角不完整但内部锚点充分的图片恢复棋盘坐标；仅由画面外位置造成的不完整不得阻止生成 FEN，响应必须列出全部规则补空位置。
- 在按棋盘、棋子套装和拍摄来源隔离的盲测集上，公开整盘零错误率、自动通过覆盖率、自动通过结果精确率、拒识能力和延迟。
- 第一阶段成熟目标为自动通过结果精确率至少 99.9%；覆盖率单独统计，不通过降低覆盖率隐藏。
- 普通 CPU 环境具有可部署的推理路径，并可选择 GPU 加速；模型替换不破坏公共 API。
- 官网根路径可直接访问；公开统计接口无需 API Key，最近记录严格受 72 小时窗口约束，累计数字跨发布保留，且返回内容无法关联到客户身份或原始文件。

## 实质未决问题

- 自有训练数据的采集授权、发布边界和长期存储政策需要在训练阶段前确定。
- 当前单机 4 核 3.6 GiB 资源能支撑受限并发的首发服务；客户量增长后的容量阈值和扩容策略仍需用生产指标确定。
- Apple 快捷指令公开分享 URL 尚待用户提供；公开文件必须保持无 Key 状态。

运行和测试见 `README.md`；生产方法见 `DEPLOYMENT.md`；当前完成度见 `docs/handoffs/CURRENT.md`。
