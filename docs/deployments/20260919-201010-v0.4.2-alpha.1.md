# RakuXQ v0.4.2-alpha.1 生产部署记录

- 时间：`2026-09-19T20:10:10+08:00`
- 目标：`raku-cn-prod-01` / `https://xq.rakubank.com`
- Git 版本：`da94c84d8ea09068816f9813282e73f112679042`
- 发布目录：`/opt/raku/portable/rakuxq/releases/da94c84d8ea09068816f9813282e73f112679042`
- 备份：`/opt/raku/backups/rakuxq/20260919-191747-da94c84d8ea09068816f9813282e73f112679042`

## 变更

- 桌面 Lab 增加分组侧边菜单和常驻主线招法轨，并重新标定 `1280×720` 棋盘尺寸。
- 顶部引擎执红/黑、分析模式、立即出招、变招、翻转、导入、保存分享和编辑入口连接真实功能。
- “新局”改为单击立即恢复标准三十二子开局，不再弹出选择窗口。
- 引擎数据面板压缩留白并保持评分、最佳着法、PV 与搜索证据的完整展示。

## 验证

- 本地：`pytest` 62 项通过，Web 单元测试 9 项通过，Ruff 与 mypy 通过。
- 浏览器：`1280×720` 下棋盘、分析面板和主线招法轨同时可见；一键新局恢复标准 FEN；侧边菜单打开和 Esc 关闭正常。
- 构建：`rakuxq_api-0.4.2a1-py3-none-any.whl`，本地 SHA-256 `ba4c3f8b23f0739be64adbd2afcddb59bd93da325d99546b3a7f40f84e1d86c6`。
- 回环健康检查：HTTP `200`，`version=0.4.2a1`。
- 公网 Lab：HTTP `200`，包含 `lab-menu-drawer`、`lab-variation` 与 `v=0.4.2a1` 资源版本。
- 服务：`rakuxq-api.service` 为 `active`，`NRestarts=0`，当前链接指向本次版本目录。
- Nginx：配置语法检查通过后完成 reload；生产密钥、12 小时审计和匿名累计数据未被部署覆盖。
- 生产 NNUE：继续保持未配置状态，页面明确显示“仅棋盘模式”，没有分发或启用受限权重。

## 回滚

将 `/opt/raku/portable/rakuxq/current` 原子切换回部署前版本，重启 `rakuxq-api.service`，再检查回环 `/healthz` 与公网 `/lab`。本次部署前副本位于上述备份目录。
