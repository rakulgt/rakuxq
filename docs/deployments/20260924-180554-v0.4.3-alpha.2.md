# RakuXQ v0.4.3-alpha.2 生产部署记录

- 时间：2026-09-24 18:05（Asia/Shanghai）
- 目标：`raku-cn-prod-01` / `/opt/raku/portable/rakuxq`
- Git 修订：`8d78cbf9a371d244ad1c044181f332ba1fd2eae8`
- 发布备份：`/opt/raku/backups/rakuxq/20260924-180316-8d78cbf9a371d244ad1c044181f332ba1fd2eae8`

## 变更

- 保持 `xq.rakubank.com` 为 Cloudflare 灰云国内直连入口。
- 增加 `vpn.xq.rakubank.com` 免费路线对照入口；部署时仍为灰云，等待完成源站验收后切换为 Cloudflare 免费代理。
- 扩展现有 Let's Encrypt ECDSA 证书，使其同时覆盖两个主机名；自动续期机制保持不变。
- Nginx 响应增加 `X-RakuXQ-Ingress`，匿名时序日志增加 `host`，不增加 IP、Key、查询参数或设备信息。

## 验证

- DNS：`vpn.xq.rakubank.com` A 记录解析为 `1.117.71.5`。
- 本地：Python 测试 `62 passed`；Web 测试 `9 passed`；Ruff 与 Mypy 通过。
- 构建：`rakuxq_api-0.4.3a2-py3-none-any.whl`，本地 SHA-256 `0a8a354f0a02d5bf85060809f864b387cedef7b69cf4b43ff334b6a8d33eb8af`。
- Nginx：`nginx -t` 通过，配置已 reload。
- 证书：`xq.rakubank.com` 与 `vpn.xq.rakubank.com` 均在 SAN 中，当前证书有效期至 2026-12-23。
- 公网：两个 `/healthz` 均返回 HTTP `200`、版本 `0.4.3a2`，且 `X-RakuXQ-Ingress` 分别返回对应主机名。
- 服务：`rakuxq-api.service` 为 `active`，`NRestarts=0`；回环健康检查为 `status=ok`。

## 后续与回滚

- 下一步由域名管理员仅将 Cloudflare 中的 `vpn.xq` A 记录切换为橙云；不得改动 `xq`。
- 切换后必须复核 HTTPS、HTTP/2/3、API 鉴权、真实图片上传耗时和限流行为，再决定是否保留。
- 应用回滚点为上述备份和上一个 `current` 发布目录；路线试验回滚只需把 `vpn.xq` 恢复灰云或删除该试验记录。
