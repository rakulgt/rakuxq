# RakuXQ v0.4.3-alpha.1 生产部署记录

- 时间：`2026-09-23T20:50:21+08:00`
- 目标：`raku-cn-prod-01` / `https://xq.rakubank.com`
- Git 版本：`de59559ba368e4dbcea65eb2fcaf56e777e324a2`
- 发布目录：`/opt/raku/portable/rakuxq/releases/de59559ba368e4dbcea65eb2fcaf56e777e324a2`
- 备份：`/opt/raku/backups/rakuxq/20260923-204115-de59559ba368e4dbcea65eb2fcaf56e777e324a2`

## 变更

- 图片接口增加标准 `Server-Timing`，分别返回鉴权、请求体接收和应用处理耗时。
- 12 小时短期审计增加鉴权、请求接收、应用处理、响应发送和总耗时五段指标，并记录请求体是否完整。
- TLS 入口启用 HTTP/2，继续复用既有 Let's Encrypt TLS 会话缓存。
- 项目 access log 改为匿名分段耗时格式，不记录 IP、API Key、查询参数、User-Agent 或设备信息。
- JSON/FEN 契约、模型权重、12 小时原图规则、Key 数据库和匿名累计统计保持不变。

## 验证

- 本地：`pytest` 62 项、Web 单元测试 9 项通过；Ruff、mypy 和 `git diff --check` 通过。
- 构建：`rakuxq_api-0.4.3a1-py3-none-any.whl`，本地 SHA-256 `f2d8dba3dbd2e17ea49bf2ce7f620dea487d3b7a5ca14b270f775695f8db7075`。
- 回环健康检查：HTTP `200`，`status=ok`，`version=0.4.3a1`，视觉 provider 已就绪。
- 公网健康检查：HTTP `200`；HTTP/2 协商结果为版本 `2`。
- 限速上传冒烟：64 KiB 占位数据以 10 KiB/s 上传，接口按预期返回 `422`，响应头为 `auth;dur=0, receive;dur=6365, app;dur=2`。
- 同一冒烟请求的短期审计记录为 `authentication=0`、`request_receive=6365`、`application=2`、`response_send=0`、`total=6368` 毫秒，且 `request_body_complete=true`。
- 匿名 access log 同步记录 `protocol=HTTP/2.0`、`request_time=6.370`、`server_timing` 分段值，未包含客户标识。
- 服务：`rakuxq-api.service` 为 `active`，`NRestarts=0`；Nginx 配置语法检查通过。
- 生产 NNUE：继续保持未配置状态，没有分发或启用受限权重。

## 回滚

将 `/opt/raku/portable/rakuxq/current` 原子切换回部署前版本，重启 `rakuxq-api.service`，再检查回环与公网 `/healthz`。Nginx、systemd 和相关托管配置的部署前副本位于上述备份目录。
