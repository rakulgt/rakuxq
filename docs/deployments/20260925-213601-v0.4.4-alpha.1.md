# RakuXQ v0.4.4-alpha.1 生产部署记录

- 时间：2026-09-25 21:36–21:40（Asia/Shanghai）
- 目标：`raku-cn-prod-01` / `https://xq.rakubank.com`
- Git 修订：`75b68b378f7bf0231e7fdc7b6a032e08149aaebd`
- 发布目录：`/opt/raku/portable/rakuxq/releases/75b68b378f7bf0231e7fdc7b6a032e08149aaebd`
- 备份：`/opt/raku/backups/rakuxq/20260925-213601-75b68b378f7bf0231e7fdc7b6a032e08149aaebd`

## 变更

- 增加方向归一化后的整盘阵营反转候选。
- 仅当唯一红帅、黑将交叉落入对方九宫，且交换阵营后阻断性违法严格减少时自动采用候选。
- 在响应警告中披露 `SEMANTIC_CAMP_INVERSION_CORRECTED`，并在原始预测元数据中保留纠正类型、原因和纠正前后阻断警告。
- 单方主帅越界、主帅缺失或交换后未改善时保持原结果并要求复核。

## 发布前验证

- `pytest`：69 项通过。
- `unittest`：26 项通过。
- 浏览器棋盘测试：9 项通过。
- Ruff、Mypy、`git diff --check`：通过。
- ONNX 模型校验：pose 与 layout 文件大小、SHA-256 和固定模型版本全部通过。
- wheel：成功构建 `rakuxq_api-0.4.4a1-py3-none-any.whl`。

## 生产验收

- systemd：`rakuxq-api.service` 为 `active`，`NRestarts=0`。
- 回环健康检查：`status=ok`、`version=0.4.4a1`、provider ready。
- 公网 HTTPS 健康检查：`200 OK`、`X-RakuXQ-Ingress=xq.rakubank.com`、版本 `0.4.4a1`。
- 使用仍在12小时审计期内的真实金色棋子皮肤失败原图，在服务器内部重跑识别；原图未离开服务器，也未加入 Git 或长期数据集。
- 旧结果为 `2n6/6c2/4Kc2r/1pp6/1pp6/1Cp6/9/R4k2b/1C2an3/1r1a2b2 b`，包含双方主帅离开九宫及士象阵营位置违法。
- 新结果为 `2N6/6C2/4kC2R/1PP6/1PP6/1cP6/9/r4K2B/1c2AN3/1R1A2B2 b`；纠正前五项阵营位置违法、纠正后零项。
- 该图片仍存在低置信棋子，因此状态保持 `review_required`；程序没有以语义纠正掩盖其他视觉不确定性。

## 回滚

将 `/opt/raku/portable/rakuxq/current` 原子切回上一版本目录，重启 `rakuxq-api.service`，然后复核回环与公网 `/healthz`。部署前备份见本文顶部路径。
