# RakuXQ v0.2.0-alpha.1 官网与匿名统计部署记录

- 时间：`2026-09-17T19:06:00+08:00`
- 域名：`xq.rakubank.com`
- 服务器：`raku-cn-prod-01`
- 发布版本：`v0.2.0-alpha.1`
- Git/服务器发布标识：`0dc86d4acc91ba8e93b51719f2c3315257fbf536`
- 备份目录：`/opt/raku/backups/rakuxq/20260917-190600-0dc86d4acc91ba8e93b51719f2c3315257fbf536`

## 变更与验证

- 根路径发布响应式中英双语产品官网，包含项目动机、发展时间线、路线图和 Apple 快捷指令预留入口。
- `/api/public/stats` 提供匿名累计指标、最近 72 小时趋势和最多 60 条最近交互；响应不含原图、Key、IP、文件名、设备或请求标识。
- 从现有 12 小时审计元数据幂等导入 11 条历史交互；公开面板验收为累计 11 条、最近 72 小时 11 条，快捷指令链接保持未配置。
- 匿名统计 SQLite 文件权限为 `0600 rakuxq:rakuxq`；事件明细 72 小时滚动删除，累计计数跨版本保留。
- `rakuxq-metrics-backup.timer` 已启用，每天约 03:17（含 10 分钟随机延迟）创建一致性快照并保留 30 天；首次有效快照为 20,480 字节、权限 `0600`。
- 本地 45 项测试、Ruff、mypy、部署脚本语法、wheel 构建和文档治理检查通过。
- 公网 `GET /` 返回 `200`，HTTP 返回 `301` 到 HTTPS；HSTS、CSP、nosniff 和 Referrer-Policy 均存在。
- 生产 `rakuxq-api.service`、匿名统计备份 timer、12 小时审计清理 timer 均为 active，`NRestarts=0`，Nginx 配置检查通过。

## 隐私边界

- 官网公开 FEN、发生时间、识别状态、置信度和推理耗时；这些字段用于展示匿名交互，不建立用户画像。
- 原图与完整交互仍只进入独立的 12 小时短期审计池，不进入公开统计库或统计备份。
- Apple 快捷指令下载内容不得包含 API Key；正式 iCloud 分享链接尚未配置。

## 回滚

把 `current` 指回 `531f8a311e235f56f7f7c05f5d3899463e0eb438`，恢复本记录中的备份配置并重启
`rakuxq-api.service`。匿名统计库、密钥库和短期审计池均位于版本目录之外，不随代码回滚删除。
