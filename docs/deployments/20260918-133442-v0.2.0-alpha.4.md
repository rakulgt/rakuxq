# RakuXQ v0.2.0-alpha.4 首页棋盘对齐修正部署记录

- 时间：2026-09-18 13:34:42 +08:00
- 发布版本：`v0.2.0-alpha.4`
- Git 修订：`d4bfa681ab315b4f8192f4001e321f2aac5a0e7e`
- 目标：`raku-cn-prod-01` / `https://xq.rakubank.com`
- 备份：`/opt/raku/backups/rakuxq/20260918-133220-d4bfa681ab315b4f8192f4001e321f2aac5a0e7e`

## 变化

- 移除首页 `.position-card` 的 `rotate(1deg)` 装饰性旋转，使示例卡片、棋盘与页面网格水平对齐。
- 将首页和开发者页面的静态资源版本参数更新为 `0.2.0a4`，避免客户端继续使用旧 CSS 缓存。

## 验证

- 本地 Ruff、48 项 Pytest、Mypy 和两个前端 JavaScript 语法检查全部通过。
- 浏览器移动宽度预览通过；首页标题、操作按钮和内容网格未发生回归。
- 公网健康检查报告 `version=0.2.0a4`；HTML 已引用 `styles.css?v=0.2.0a4`。
- 公网 CSS 中 `.position-card` 为 `transform: none`，且不再包含 `rotate(1deg)`。
- 服务为 `active`，`NRestarts=0`，当前版本目录为本次 Git 修订。

## 回滚

将 `/opt/raku/portable/rakuxq/current` 指向上一版本目录，重启 `rakuxq-api.service`；必要时从上述备份恢复受影响配置。
