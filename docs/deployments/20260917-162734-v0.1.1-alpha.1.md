# RakuXQ v0.1.1-alpha.1 首次生产部署记录

- 时间：`2026-09-17T16:27:34+08:00`
- 域名：`xq.rakubank.com`
- 服务器：`raku-cn-prod-01`
- 发布版本：`v0.1.1-alpha.1`
- 生产提交：`4b7c232df1acfb3d55f6b62e7460e1c8bfdfbb1c`
- 发布目录：`/opt/raku/portable/rakuxq/releases/4b7c232df1acfb3d55f6b62e7460e1c8bfdfbb1c`
- 备份目录：`/opt/raku/backups/rakuxq/20260917-162408-4b7c232df1acfb3d55f6b62e7460e1c8bfdfbb1c`

## 部署内容

- FastAPI + ONNX Runtime 服务由 `rakuxq-api.service` 托管，只监听 `127.0.0.1:8040`。
- Nginx 提供公网反向代理、上传限制、每 Key 限流和并发限制。
- 客户 API Key 使用独立有效期；服务端只保存 SHA-256 哈希。
- 到期响应提供微信 `lgtqcn` 和 `39 元人民币/365 天` 的续费信息。
- Let's Encrypt 为 `xq.rakubank.com` 签发 ECDSA 证书，有效期至 `2026-12-16T07:18:15Z`；Certbot 定时续期已启用。
- Nginx 配置拆分为可复用路由片段和首次部署/TLS 两种模板，后续发布不会覆盖源站证书配置。

## 验证证据

- 本地：`pytest` 34 项通过，Ruff 通过，mypy 通过。
- 模型：pose/layout 文件大小和 SHA-256 与 `models/manifest.json` 一致。
- DNS：`xq.rakubank.com` 解析到 Cloudflare 代理地址 `104.21.17.51`、`172.67.222.81`。
- 公网：`GET https://xq.rakubank.com/healthz` 返回 `status=ok`，provider 与鉴权均 ready。
- 鉴权：无 Key 的识别请求返回 HTTP `401` 和 `API_KEY_REQUIRED`。
- 真实图片：公网 JSON 接口返回 HTTP `200`、`status=accepted`，FEN 为
  `3k5/9/9/9/9/9/3p5/2pANR3/3KNR3/3ACC3 w - - 0 1`，与已确认基准完全一致。
- 公网端到端计时：一次含图片上传、Cloudflare 转发、推理和 JSON 返回的测量为 `3.015496s`；它不是纯模型推理耗时。
- 源站：通过 `127.0.0.1:443` + SNI 的 HTTPS 健康检查；证书主题为 `CN=xq.rakubank.com`。
- 服务：`rakuxq-api.service` 为 `active`，`NRestarts=0`，验收时内存约 `222 MiB`；`nginx -t` 通过。

## 已知后续项

公网 HTTPS 已生效；验收期间 Cloudflare 行为表明该 zone 仍以 Flexible 模式回源。TLS 源站已经准备好，
应把 Cloudflare SSL/TLS 加密模式切换为 **Full (strict)**，再复核公网健康检查，以确保 API Key 在
Cloudflare 到源站这一段同样加密。为不中断现有调用，Nginx 在过渡期同时保留 80 和 443 反向代理。

## 回滚

把 `/opt/raku/portable/rakuxq/current` 指回上一版本
`4517918438b31d1da808dc64839be77226fa867f`，恢复本记录备份目录中的 Nginx/systemd 配置，执行
`nginx -t` 后 reload Nginx，并重启 `rakuxq-api.service`。API Key 数据库位于独立 secrets 目录，
不随版本回滚或覆盖。
