# RakuXQ v0.1.2-alpha.2 URL 行棋方别名部署记录

- 时间：`2026-09-17T18:16:07+08:00`
- 域名：`xq.rakubank.com`
- 服务器：`raku-cn-prod-01`
- 发布版本：`v0.1.2-alpha.2`
- Git/服务器发布标识：`178ca383258ce1d120f86a7219b650451d544f47`
- 发布目录：`/opt/raku/portable/rakuxq/releases/178ca383258ce1d120f86a7219b650451d544f47`
- 备份目录：`/opt/raku/backups/rakuxq/20260917-181607-178ca383258ce1d120f86a7219b650451d544f47`

## 部署内容

- `side_to_move` 同时接受 `red/black`、`w/b`、空格前缀的 `w/b` 和 URL 就绪值
  `%20w/%20b`，内部统一规范化为红方或黑方。
- Apple 快捷指令可以把同一个菜单值同时用于 API 表单和 xiangqiai.com 棋谱 URL，不再需要
  增加条件判断或文本替换步骤。
- 输出 FEN 继续使用标准空格分隔与 `w/b` 行棋方字段。

## 验证证据

- 本地 `pytest` 40 项通过；Ruff、mypy 和 `git diff --check` 通过。
- 公网以真实图片和字面值 `%20w` 调用 `/v1/recognitions`，HTTP 请求通过参数校验并完成模型推理。
- 响应将 `side_to_move` 规范化为 `red`，FEN 以 `w - - 0 1` 结尾；该样本因模型置信度返回
  `review_required`，与本次参数兼容性无关。
- 公网健康检查返回 `status=ok`、`version=0.1.2a2`，provider 与鉴权均 ready。

## 回滚

把 `/opt/raku/portable/rakuxq/current` 指回
`5344423b469198603c2a9d8f6b899cba79ac6d6a`，恢复备份目录中的配置并重启
`rakuxq-api.service`；短期审计目录与密钥库均位于版本目录之外，不受回滚影响。
